# ─── api-boraviajar (Spring Boot) — variáveis típicas no systemd / Docker / .env local ───
# JWT_SECRET deve ser BYTE A BYTE igual ao JWT_SECRET do bora_viajar (.env do Node).

PORT=8080

# Mesmo segredo que no .env do Node (bora_viajar)
JWT_SECRET=1af55cfec570e163acf5d82c860acff4212097e76757027ddbc6465d6a1a9278

# Opção A — JDBC numa linha (ajusta host, user, password, encoding da password)
DATABASE_URL=jdbc:mysql://HOST:3306/boraviajar_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo
DB_USERNAME=rotaeasy_user
DB_PASSWORD=gTvE3ti!QQn!e9x

# Opção B — se preferires só user/password e URL fixa no application.yml, mantém só estes três + JWT_SECRET.

# ─── Opcional (push web) ───
# VAPID_PUBLIC_KEY=
